# How to test

For testing the `str_enum` feature, use the following command:

```bash
uv run pytest tests/test_str_enum_flag.py
```

if you are not using `uv`, you can use the following command:
```bash
python -m pytest tests/test_str_enum_flag.py
```